import { OrchestratorConfig } from './types.js';
import { RagManager } from './rag.js';
export declare class ChatOrchestrator {
    private groq;
    private config;
    private memory;
    private rag;
    constructor(config: OrchestratorConfig);
    getRagManager(): RagManager;
    private buildSystemPrompt;
    streamChat(userMessage: string): AsyncGenerator<string, void, unknown>;
}
