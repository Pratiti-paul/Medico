import { ChatMessage } from './types';
export declare class MemoryManager {
    private history;
    private maxHistoryLimit;
    constructor(maxHistoryLimit?: number);
    addMessage(message: ChatMessage): void;
    getHistory(): ChatMessage[];
    clear(): void;
}
