import { DocumentChunk } from './types';
export declare class RagManager {
    private chunks;
    private enabled;
    constructor(enabled?: boolean);
    search(query: string, topK?: number): Promise<DocumentChunk[]>;
    ingestDocument(text: string, metadata?: any): void;
}
